# --- Python ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.nox/
*.cover

# --- Environment / secrets ---
.env
.env.*
!.env.example

# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride

# --- IDE ---
.idea/
.vscode/
*.swp
*.swo
*~

# --- Auth keys & pairing material (NEVER commit) ---
key.hex
*.key
auth_key.*
pairing.json

# --- Personal / device-specific BLE data ---
# GATT dumps: CoreBluetooth UUIDs, ring name, manufacturer payloads
services.json
services-*.json
oura_gatt_dump.json
*.timeline.json

# BLE session logs (hex payloads, nonces, battery readings)
*.jsonl
captures/

# PacketLogger / Wireshark captures (raw HCI traffic)
*.pklg
*.pcap
*.pcapng

# mitmproxy / correlation output (tokens, health metrics)
correlation.json

# Frida / RE scratch output
frida-*.log
dump/

# Local Wireshark plugin symlink (install via ln -sf, do not commit)
# ~/.local/lib/wireshark/plugins/oura.lua
